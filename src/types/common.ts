export interface JetTypeInterface {
  speed: number;
  scale: number;
  health: number;
}

export interface MissileType {
  speed: number;
  damage: number;
  scale: number;
  fireInterval: number;
}
